#ifndef AGEMANIPULATION_H
#define AGEMANIPULATION_H

extern int year;
extern int month;
extern int day;
extern int currentDay;
extern int currentMonth;
extern int currentYear;

// Calculates the exact age (years, months, days) based on the date of birth.
void exactAge();

// Displays the exact age in years, months, and days.
void displayExactAge();

// Displays the age in years.
void ageInYears();

// Displays the age in month.
void ageInMonths();

// Displays the age in month.
void ageInWeeks();

// Displays the age in Days.
void ageInDays();

#endif